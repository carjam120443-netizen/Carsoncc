# AndroidX is enabled for the CarsonCC Mobile app.
android.useAndroidX=true
android.enableJetifier=true

# Keep Gradle builds stable in CI and local environments.
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
